---
name: backlog-import
description: Diagnostica import sem escrever dados.
argument-hint: "--db PATH"
user-invocable: true
disable-model-invocation: true
---
# Importar

Execute `backlogctl [--json] doctor --db PATH`. Import está indisponível; não execute `import`, não crie/atualize itens e termine explicando a indisponibilidade. Nunca simule envelope de sucesso.