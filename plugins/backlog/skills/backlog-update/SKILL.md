---
name: backlog-update
description: Diagnostica runtime sem instalar ou alterar binário.
argument-hint: "--db PATH"
user-invocable: true
disable-model-invocation: true
---
# Atualizar

Execute `backlogctl [--json] doctor --db PATH` e, se necessário, `backlogctl --version`. Update está indisponível; não execute `update`, não baixe nem instale binário e termine explicando a indisponibilidade, sem mutar.